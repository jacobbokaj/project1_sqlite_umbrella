defmodule Project1SqliteWeb.CarsUpdateComponent do
  def car_update(
        table_name,
        car_id,
        update_id,
        update_brand,
        update_model,
        update_year,
        update_price
      ) do
  end

  def cars_filter_ids() do
  end
end
